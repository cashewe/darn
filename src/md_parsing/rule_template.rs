// rule_template:
//  - things like 'everything inside tag must be kept together' as a generic rule which can then be fed along with the tag to the rule object
//  - this might just be a function with a set trace rather than an object tbh